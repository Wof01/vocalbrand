streamlit>=1.32.0
requests>=2.31.0
stripe>=7.0.0
pydub>=0.25.1
python-dotenv>=1.0.0
#simpleaudio>=1.0.4   #commented just for deploy compatibility in local uncomment
streamlit-audiorecorder>=0.0.2
sentry-sdk>=2.0.0
passlib[bcrypt]>=1.7.4  # add explicit passlib so bcrypt scheme works predictably across envs
fastapi>=0.111.0         # webhook_server (Stripe webhooks)
uvicorn[standard]>=0.30.0 # to run FastAPI webhook server
python-multipart>=0.0.9   # ensure FastAPI can parse form-data if needed
typing-extensions>=4.12.0 # forward compatibility (some libs rely on it)
pytest>=8.2.0             # optional: run tests (e.g., tests/test_engine_offline.py)
git+https://github.com/stefanrmmr/streamlit_audio_recorder.git@777d18114130137d492c0378a86631fff1ff1be5#egg=streamlit-audiorec  # optional recorder component (pinned commit)

